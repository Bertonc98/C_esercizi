#include<stdio.h>

int main()
{
	int n, sum=0, i;

	for(i=0; i<10; i++){
		scanf("%d", &n);
		if(n==0)
			break;
		sum+=n;
	}

	if(i!=0)
		printf("VIoletta deeppussyblowjob\n");
	else
		printf("%d\n",sum);
}